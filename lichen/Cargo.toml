[package]
name = "lichen"
version = "0.1.0"
edition = "2024"
description = "One opinionated look applied to foliage, distilled from the foliage repo's own site -- not part of foliage_proper's unopinionated composite/ module on purpose."
license = "MIT OR Apache-2.0"

[dependencies]
foliage = { path = "../foliage" }
