# Introduction

This book is not written yet.

The scaffold is here so that `cargo xtask book` builds and publishes to `docs/book`, and so the
chapters have somewhere to land. Until they do, the
[API reference](https://eblack-leaf.github.io/foliage/api/foliage/) is the documentation: every
type in the public surface carries what it is for and why it is shaped the way it is, and the
crate root is the map.
