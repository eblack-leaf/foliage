use crate::coordinate::position::Position;
use crate::coordinate::LogicalContext;

pub struct Animation {
    pub(crate) adjust: Position<LogicalContext>,
    // interpolation-handles
    // timeline
}
