// AUTO-GENERATED by foliage_icons -- do not edit by hand.
// Regenerate via: foliage_icons gen --svg <svg-dir> --out <this-dir> --size 24 --mips 3

use foliage::{icon_handle, load_asset, EcsExtension, Foliage, Icon, IconId};

#[icon_handle]
pub enum IconHandles {
    ArrowUp,
    BookOpen,
    Box,
    ChevronDown,
    Code,
    Github,
    Layers,
    Menu,
    Play,
    Repeat,
    Search,
    Shuffle,
    SkipLeft,
    SkipRight,
    Terminal,
    X,
}

pub const TEXTURE_SCALE: (f32, f32) = (96.0, 96.0);
pub const RENDER_SIZE: (f32, f32) = (24.0, 24.0);
pub const MIP_COUNT: u32 = 3;

pub fn register(foliage: &mut Foliage) {
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::ArrowUp), include_bytes!("arrow-up.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::BookOpen), include_bytes!("book-open.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::Box), include_bytes!("box.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::ChevronDown), include_bytes!("chevron-down.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::Code), include_bytes!("code.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::Github), include_bytes!("github.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::Layers), include_bytes!("layers.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::Menu), include_bytes!("menu.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::Play), include_bytes!("play.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::Repeat), include_bytes!("repeat.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::Search), include_bytes!("search.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::Shuffle), include_bytes!("shuffle.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::SkipLeft), include_bytes!("skip-left.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::SkipRight), include_bytes!("skip-right.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::Terminal), include_bytes!("terminal.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
    foliage.world.spawn(Icon::memory_sized(IconId::from(IconHandles::X), include_bytes!("x.icon"), TEXTURE_SCALE.into(), RENDER_SIZE.into(), MIP_COUNT));
}
