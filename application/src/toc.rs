use foliage::{Entity, Tree};

/// Placeholder second route -- reachable via the global chrome's explicit ToC button (and,
/// since `Router` treats every route the same, via the main forward/back navigator too).
/// Intentionally empty: nothing to show yet, the real table-of-contents content lands once
/// the themed section pages it links to actually exist.
pub fn toc(_tree: &mut Tree, _slot: Entity) {}
