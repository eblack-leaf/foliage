[package]
name = "application"
version = "0.1.0"
edition = "2024"

[lib]
name = "application"
crate-type = ["cdylib", "rlib"]

[dependencies]
foliage = { path = "../foliage" }
tracing = "0.1.40"
tracing-subscriber = "0.3.18"
